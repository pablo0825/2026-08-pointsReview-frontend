interface:
  display_name: "Cogito"
  short_description: "Adopt, clarify, and execute software work"
  default_prompt: "Use $cogito to govern one Feature Slice or narrow Maintenance operation. Include $cogito again in every later message that continues the workflow."

policy:
  allow_implicit_invocation: false
