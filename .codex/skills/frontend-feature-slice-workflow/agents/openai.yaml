interface:
  display_name: "Frontend Feature Slice Workflow"
  short_description: "Plan, implement, verify, and accept frontend slices"
  default_prompt: "Use $frontend-feature-slice-workflow to create or execute a frontend Feature Slice with explicit approval gates."
